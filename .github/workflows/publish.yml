name: Publish to npm

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write    # creating GitHub Releases
  id-token: write    # OIDC token for npm provenance
  packages: write    # publishing to GitHub Packages

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      # npm 11.5.1+ does the GitHub OIDC → npm token exchange for trusted
      # publishing automatically. The npm bundled with Node 20 is 10.x,
      # which can sign provenance but can't auth the PUT. Force a recent
      # CLI so `npm publish --provenance` works without an NPM_TOKEN.
      - name: Upgrade npm to a Trusted-Publishing-capable version
        run: npm install -g npm@latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run tests
        run: bun test

      - name: Typecheck
        run: bun run typecheck

      - name: Verify tag matches package.json version
        run: |
          tag="${GITHUB_REF#refs/tags/v}"
          pkg=$(node -p "require('./package.json').version")
          if [ "$tag" != "$pkg" ]; then
            echo "Tag v$tag does not match package.json version $pkg" >&2
            exit 1
          fi

      - name: Publish to npm with provenance
        run: npm publish --provenance --access public

      # Mirror to GitHub Packages so the package shows up under the repo's
      # Packages sidebar on github.com. setup-node rewrites ~/.npmrc to
      # point at npm.pkg.github.com, then `npm publish` picks up
      # NODE_AUTH_TOKEN for auth. No --provenance here: GitHub Packages
      # has its own attestation flow, not the Sigstore one npmjs uses.
      - name: Reconfigure npm registry for GitHub Packages
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@12og3r'

      - name: Publish to GitHub Packages
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
